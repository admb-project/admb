name: ubuntu-22.04

on: [push]

jobs:
  build:

    runs-on: ubuntu-22.04
    
    steps:
    - uses: actions/checkout@v3
    - name: flex
      run: sudo apt-get install flex
    - name: clean
      run: |
        CC=gcc-7 CXX=g++-7 make clean
    - name: build
      run: |
        CC=gcc-7 CXX=g++-7 make
    - name: debug
      run: |
        CC=gcc-7 CXX=g++-7 make DEBUG=yes
    - name: shared
      run: |
        CC=gcc-7 CXX=g++-7 make shared
    - name: zip
      run: |
        CC=gcc-7 CXX=g++-7 make zip
    - name: Artifact
      uses: actions/upload-artifact@v1
      with:
        name: admb_ubuntu_zip
        path: build/admb.zip
    - name: tests 
      run: CC=gcc-7 CXX=g++-7 make verify DEBUG=yes TESTS_REMOTE=no
    - name: Artifact
      uses: actions/upload-artifact@v1
      with:
        name: outputs
        path: outputs.txt
