sudo: require
language: cpp

git:
  depth: false

branches:
  only:
    - master

notifications:
  email: false

compiler:
  - gcc

os:
  - linux
  - osx

jobs:
  include:
  - stage: build
    script: make clean dist
  - stage: test
    script: make --directory=examples admb
