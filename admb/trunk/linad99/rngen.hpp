class ran_number_generator
{
  long int idum;
  long idum2;
  long iy;
  ivector iv;
  double ran_number_generator::better_rand(void);
  ran_number_generator(int seed);
  reinitialize(int seed);
};

double randn(_CONST random_number_gnerator& rng);
