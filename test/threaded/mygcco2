CXX=g++
#ADMB_HOME=/home/davef/admodel
echo "!!! Note using the version found in directory "
echo ${ADMB_HOME} 
${CXX} -Wall -g -pthread -fpermissive -DOPT_LIB -DUSE_PTHREADS -DUSE_LAPLACE -Dlinux \
  -D__GNUDOS__ -o$1 $1.cpp  \
  df_file.cpp \
  model7.cpp \
   -I${ADMB_HOME}/include -I. -I/usr/include \
  -L${ADMB_HOME}/lib -L/usr/lib \
  -ladt -lado -ladmod -ldf1b2o -ladt -lado -ladmod -ldf1b2o \
  -ladt -lado -ladmod -ldf1b2o -ladt -lado -ladmod -ldf1b2o
echo ${ADMB_HOME}/include 
